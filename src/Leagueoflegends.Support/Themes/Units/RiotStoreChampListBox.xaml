<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:james="using:Jamesnet.WinUI3"
    xmlns:units="using:Leagueoflegends.Support.UI.Units">
  
  <Style TargetType="units:RiotStoreChampListBox">
    <Setter Property="VerticalAlignment" Value="Top"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="units:RiotStoreChampListBox">
          <ScrollViewer x:Name="ScrollViewer" HorizontalScrollBarVisibility="Disabled">
            <ItemsPresenter/>
          </ScrollViewer>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ItemsPanel">
      <Setter.Value>
        <ItemsPanelTemplate>
          <james:CustomWrapPanel x:Name="PART_WrapPanel"/>
        </ItemsPanelTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
